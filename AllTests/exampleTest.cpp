//
// Unit test for example of failure to compile with <optional>
//

#include "CppUTest/TestHarness.h"

#include "example.hpp"

TEST_GROUP(example)
{
    void setup() {
    }
    void teardown() {
    }
};

TEST(example, nextUnconnComplete)
{
}
